apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "teste-app.fullname" . }}-test-connection"
  labels:
    {{- include "teste-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "teste-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
